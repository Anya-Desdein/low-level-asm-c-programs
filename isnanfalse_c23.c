#include <stdio.h>
#include <math.h>
#include <stdbool.h>

int main() {
	double a = NAN;
	int *b = NULL;
	int c = 0;
	float d = 0;

	printf("Checking if bool %d, %d, %d, %d \n", (bool)a, (bool)b, (bool)c, (bool)d);

}


